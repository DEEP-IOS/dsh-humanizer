# 安全、隐私与漏洞报告

适用于 0.4 记忆工作台；0.3 不提供这里描述的持久记忆能力。

## 数据流与存储

插件在官方 DSH_HOME 下创建 plugins/dsh-humanizer/memory.sqlite，使用本机 SQLite。保存作品名、公开任务交接、关系记录、引文、来源标签和来源文本哈希。source_text 全文不写入该记忆数据库，但工具参数仍可能被宿主日志记录。

插件自身不调用网络、额外模型或向量服务；返回资料会进入 DSH 会话与所配置模型的上下文。因此不能把本机数据库理解为模型提供商看不到内容。共享机器应使用操作系统账户和文件权限保护数据；数据库不提供加密或独立身份认证。

工作区与作品是逻辑隔离边界，不抵御同一操作系统用户读取数据库。工作区标识来自宿主 Session 的真实工作目录；模型不能提交任意数据库路径。

## 来源与工具权限

引文校验只证明原句包含在本次提供的来源文字中，不能证明真实或推断正确。记忆中的命令是引用资料，不获得系统指令权限；通过动态用户上下文提供，不混入可信 system section。

humanize_reference 只读取包内参考文档。记忆工具不自动扫描稿件。独立 guard CLI 会读取用户明确提供的两个文件路径，与模型 reference 工具的路径边界不同。

同 key 冲突保留候选，修正和遗忘需要当前版本；这些检查不保证发现所有语义冲突。不得伪造来源来通过校验。

## 遗忘、备份与卸载

forget 清除指定 key 的历史负载，留下 key、版本与 forgotten 墓碑。SQLite 使用 secure_delete；这不保证清除 WAL、文件系统快照、备份、宿主日志或模型服务副本中的全部痕迹。其他关系和交接可能仍引用同一事实。

卸载保留数据。文件备份前关闭所有使用该库的 DSH 进程；完整清理应分别管理插件数据、宿主日志与备份。不要在活跃进程下覆盖数据库。

## 报告漏洞

若仓库已启用 GitHub 私密漏洞报告，请使用仓库 Security 页面中的私密报告入口。若入口不可用，公开 issue 仅请求维护者提供私密联系渠道，不附利用细节、数据库、稿件或凭据。本仓库不在此承诺固定响应时限或未经确认的报告邮箱。

提供受影响版本、环境、最小脱敏复现、影响与缓解办法。重点关注参数边界、参考路径限制、工作区隔离、来源指令提升、遗忘、并发和供应链。修复应记录受影响版本及升级方式，公开说明避免暴露用户数据。
